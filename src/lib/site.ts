/** URL of the Chinese storefront (set in production, e.g. https://teyvat-closet.cn) */
export const cnSiteUrl =
  process.env.NEXT_PUBLIC_CN_SITE_URL ?? "http://localhost:3000";

/** URL of the global English storefront */
export const intlSiteUrl =
  process.env.NEXT_PUBLIC_INTL_SITE_URL ?? "http://localhost:3001";
